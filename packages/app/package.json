{
  "name": "@htmleditor/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run build --workspace @htmleditor/core",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "npm run build --workspace @htmleditor/core",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@htmleditor/core": "0.0.0",
    "grapesjs": "^0.23.2"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
